Процедура Задание3()

	Коллекция = Новый СписокЗначений;
	
	Коллекция.Добавить("1");

	Коллекция.Добавить("22");

	Коллекция.Добавить("333");

	Коллекция.Добавить("333");

	Коллекция.Добавить("22");

	Коллекция.Добавить("1");

	Коллекция.Добавить("22222222222222");  
	
	Флаг = 1;
	Индекс = 0;	
	
	
	Пока Флаг <> 0  Цикл     //Находим первый,удовлетворяющий нас элемент
	
		Если СтрДлина(Строка(Коллекция[Индекс])) > 2 Тогда
		
			 Коллекция.Удалить(Коллекция[индекс]);
			 
			 Если Коллекция.Количество() = 0 Тогда
			 
			 	 прервать;
			 
			 КонецЕсли; 
		
		 Иначе 
			 Флаг = 0;
	    КонецЕсли;
	КонецЦикла;  
	
	Флаг = 1;
	Индекс = 1;
	
	Пока Флаг <> 0  Цикл
		
		 Строка = Строка(Коллекция[индекс]);
		 //Строка = Строка(Коллекция.Получить(Индекс)); 
		 Если СтрДлина(Строка) > 2  Тогда
			 
			 Коллекция.Удалить(Коллекция[Индекс]);
			 
		 Иначе
			 
			 Индекс = Индекс + 1;
			 
		 КонецЕсли;
		 Если Индекс > (Коллекция.Количество() - 1) Тогда
			 
			 	Флаг = 0;
				
		 КонецЕсли; 

	 КонецЦикла;  

	
	
	
	Для каждого Элемент Из Коллекция Цикл
	
		Сообщить(
			Элемент.Представление + " : " + Строка(Элемент.Значение)
		);
	                           
	КонецЦикла; 

КонецПроцедуры
